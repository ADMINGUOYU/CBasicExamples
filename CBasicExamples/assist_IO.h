#ifndef _ASSIST_IO_H_
#define _ASSIST_IO_H_

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int get_int(void);
int inputNUMRange(int min, int max);
size_t get_size_t(void);

#endif
